// function declaration

function getName(person) {
    return `${person.name} ${person.surname}`;
}

// function expression

const getMame = function (person) {
    return `${person.name} ${person.surname}`;
}

// arrow function

const getName = (person) => {
    return `${person.name} ${person.surname}`;
}
